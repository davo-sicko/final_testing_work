# Журнал поддержки

| ID | Описание | Шаги воспроизведения | Решение | Версия |
|----|----------|------------------------|---------|--------|
| BUG-001 | Пустое имя автора принимается при добавлении книги без ошибки | 1. Запустить `python menu.py`<br>2. Выбрать "Добавить книгу"<br>3. Название: `Title`, Автор: оставить пустым (пробел), Год: `2020`<br>4. Книга добавляется со значением `author=""` | В `logic.add_book` добавлена проверка на пустую/пробельную строку `author`, аналогичная проверке `title`. Покрыто тестами `test_bug001_*` в `tests/test_bugfixes.py`. | 1.0.1 |
| BUG-002 | `update_book` принимает произвольную строку в поле `status`, из-за чего книга пропадает из обоих фильтров (`read`/`unread`) | 1. Добавить книгу<br>2. Вызвать `logic.update_book(id, status="something_invalid")`<br>3. Запустить фильтр по `read` и по `unread` — книга не отображается ни в одном | В `logic.update_book` добавлена проверка: `status` должен быть одним из `("read", "unread")`, иначе выбрасывается `ValueError`. Покрыто тестами `test_bug002_*` в `tests/test_bugfixes.py`. | 1.0.1 |
| IMPROVEMENT-001 | Запрос: добавить сортировку списка книг по году издания для удобства просмотра | — | Добавлена функция `logic.get_all_books_sorted_by_year()` и пункт меню "8. Показать книги по году издания". Покрыто тестами `test_get_all_books_sorted_by_year*` в `tests/test_logic.py`. | 1.0.2 |
| CHANGE-001 | Запрос на изменение поведения: поиск должен находить книги, даже если запрос введён в другом регистре или с лишними пробелами по краям | Поведение уже было реализовано в `search_books` (приведение к `.lower()` и `.strip()`), отдельных изменений не требовалось — подтверждено регрессионным прогоном `test_search_by_title_case_insensitive` | Закрыто как "уже работает", дано пояснение в Issue с указанием на существующий тест. | — |
| QUESTION-001 | Вопрос: как добавить книгу, если год издания неизвестен? | — | Ответ дан в Issue и продублирован в README: на данный момент поле `year` обязательно; пользователю предложено использовать примерный/предполагаемый год как временное решение. Зафиксировано как возможное направление для будущего улучшения (необязательное поле `year`). | — |
